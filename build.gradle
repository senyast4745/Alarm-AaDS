plugins {
    id 'java'
}

group 'com.senyasdt4745'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

jar {
    manifest {
        attributes 'Main-Class': 'com.senyasdt4745.technopolisTeme.src.main.java.MainAlarm'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}
